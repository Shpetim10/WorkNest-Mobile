import React from 'react';

import { SessionExpiredScreen } from '@/features/auth';

/**
 * Session Expired Screen Entry Point
 */
export default function SessionExpiredPage() {
  return <SessionExpiredScreen />;
}
